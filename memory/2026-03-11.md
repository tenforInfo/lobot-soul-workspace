
## Telegram bot troubleshooting (default + miehome)
- User reported another Telegram bot had no response while slash commands could respond intermittently.
- Confirmed repeated gateway error in logs: `gateway/channels/telegram handler failed: Error: Session file path must be within sessions directory`.
- Upgraded OpenClaw to `2026.3.8`; issue persisted after upgrade.
- Found invalid `sessionFile` entries in `/Users/tensorinfo/.openclaw/agents/main/sessions/sessions.json` pointing to legacy path `~/.clawdbot/...` (cross-directory).
- Backed up sessions store and removed 3 invalid cross-directory Slack session entries; restarted gateway.
- After cleanup/restart, new Telegram `ping` messages were processed and no fresh `Session file path must be within sessions directory` error appeared in latest tail.
- Separate non-blocking issue observed: CLI showed `device signature invalid` when connecting to local gateway; gateway channels themselves were still running.

- Post-fix validation: default Telegram bot accepted `ping` and showed embedded run starts without immediate path error after restart.
- However, after user sent a message to `miehome`, error recurred at `2026-03-11T01:18:00.332Z`: `gateway/channels/telegram handler failed: Error: Session file path must be within sessions directory`.
- Re-checked both session stores (`agents/main/sessions/sessions.json` and `agents/miehome/sessions/sessions.json`): no out-of-directory `sessionFile` entries found, indicating remaining bug likely comes from runtime session resolution path not directly visible in current stores.
